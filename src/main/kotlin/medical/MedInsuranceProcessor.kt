package medical

import YearlyDetail
import config.SimConfig
import expense.ExpenseRec

object MedInsuranceProcessor {
    fun process(config: SimConfig, currYear: YearlyDetail): List<ExpenseRec> = ArrayList()
}